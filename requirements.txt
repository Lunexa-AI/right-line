# Core Dependencies for RightLine MVP (Vercel Serverless Optimized)
# Lightweight dependencies under 250MB Lambda limit

# Framework (Serverless)
fastapi==0.111.0
mangum==0.17.0
pydantic==2.8.0
pydantic-settings==2.4.0

# HTTP & Async
httpx==0.27.0
aiofiles==24.1.0

# Document Processing (Lightweight)
beautifulsoup4==4.12.3
# Removed lxml (heavy C extensions) - using html.parser instead

# AI & Vector Store (HTTP API only)
openai==1.3.7
# Removed pymilvus + grpcio (heavy) - using HTTP API instead

# Utilities
orjson==3.10.6
python-multipart==0.0.9
tenacity==8.5.0
structlog==24.4.0
python-dotenv==1.0.1

# Monitoring (Minimal)
# Removed sentry-sdk[fastapi] (heavy) - using basic logging instead
